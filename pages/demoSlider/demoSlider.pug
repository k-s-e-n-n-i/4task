include /blocks/rangeSlider/rangeSlider.pug

include /blocks/checkboxList/checkboxList.pug
include /blocks/inputText/inputText.pug
include /blocks/radiogroup/radiogroup.pug

include /pages/demoSlider/mixin_demoSlider.pug


div.slider

  div.slider__item.slider1
    +rangeSlider(
      {
        topic : "Обычный, диапазон",
        labelMin : 0,
        labelMax : 10000,
        id : "idSlider1",
        postText : ""
      }
      )
    +sliderConfig(
      {
        id : "1"
      }
      )

  div.slider__item.slider2
    +rangeSlider(
      {
        topic : "С шагом, от начала до Х",
        labelMin : 0,
        labelMax : 10000,
        id : "idSlider2",
        postText : ""
      }
      )
    +sliderConfig(
      {
        id : "2"
      }
      )

  div.slider__item.slider3
    +rangeSlider(
      {
        topic : "Обычный, единичное значение",
        labelMin : 0,
        labelMax : 10000,
        id : "idSlider3",
        postText : ""
      }
      )
    +sliderConfig(
      {
        id : "3"
      }
      )

  div.slider__item.slider4
    +rangeSlider(
      {
        topic : "С шагом, диапазон",
        labelMin : 0,
        labelMax : 10000,
        id : "idSlider4",
        postText : ""
      }
      )
    +sliderConfig(
      {
        id : "4"
      }
      )

  div.slider__item.slider5
    +rangeSlider(
      {
        topic : "Обычный, от начала до Х",
        labelMin : 0,
        labelMax : 10000,
        id : "idSlider5",
        postText : ""
      }
      )
    +sliderConfig(
      {
        id : "5"
      }
      )
    

  

script(src="/pages/demoSlider/demoSlider.ts")
div(id="mocha")